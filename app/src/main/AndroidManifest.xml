<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projpuc.salaodebeleza">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.salaodebeleza"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activitys.CadastroActivity"
            android:exported="true" >
        </activity>

        <activity
            android:name=".activitys.ConsultaActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".activitys.MenuActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activitys.NewLoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>